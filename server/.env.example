# Copy to /etc/bfrs-api.env on the Hetzner server and fill in real values.
# Mode 0600, owner root. Loaded by the systemd unit via EnvironmentFile=.

# Pulled from your current Netlify env vars (Site settings -> Environment variables).
GROQ_KEY=
RESEND_KEY=
SUPABASE_SERVICE_KEY=

# Optional overrides — defaults baked into server/index.js are fine for production.
# SUPABASE_URL=https://tohhqssnngvavkkeqzfl.supabase.co
# KEEPALIVE_KEY=bfrs-keepalive-2026
# PORT=8080
# HOST=127.0.0.1
